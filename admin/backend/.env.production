# 生产环境配置
# 将此文件上传到服务器，并在部署脚本中加载

# Flask 配置
FLASK_APP=run.py
FLASK_ENV=production
FLASK_DEBUG=False
FLASK_HOST=0.0.0.0
FLASK_PORT=5000

# 安全密钥（在生产环境中必须更改！）
SECRET_KEY=change-this-to-a-random-secret-key-in-production
JWT_SECRET_KEY=change-this-to-another-random-secret-key-in-production

# 数据库配置（根据实际 MySQL 设置调整）
DATABASE_URL=mysql+pymysql://root:your_mysql_password@localhost/student_saas_admin

# CORS 配置（允许的域名，用逗号分隔）
CORS_ORIGINS=https://charonspace.asia,https://www.charonspace.asia

# 文件上传路径（确保服务器上存在此目录）
UPLOAD_FOLDER=/var/www/student_saas/admin/backend/app/static/uploads

# 日志配置
LOG_LEVEL=INFO
LOG_FILE=/var/log/student_saas_admin.log

# SSL 配置（如果使用反向代理如 Nginx，则不需要）
SSL_CERT_PATH=/etc/ssl/certs/charonspace.asia.crt
SSL_KEY_PATH=/etc/ssl/private/charonspace.asia.key
